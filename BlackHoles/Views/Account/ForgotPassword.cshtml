@model BlackHoles.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "Забыли пароль?";
}

<h2>@ViewBag.Title</h2>

<p>Данная страница позволяет вам сбросить пароль (заменить на новый). После ввода адреса электронной почты (который был использован при регистрации) 
  на него будет выслано письмо, содержащее ссылку для сброса пароля. Откройне это письмо и перейдите по ссылке. Там вам будет предложено указать новый пароль.</p>

@using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        <h4>Введите адрес электронной почты <b>использованный при регистрации</b>:</h4>
        <hr />
        @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control fixed-width" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Послать сслыку для сброса пароля" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
