@model BlackHoles.Models.RegisterViewModel
@{
    ViewBag.Title = "Регистрация";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Создание новой учетной записи (нового пользователя)</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.UserName, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.UserName, new { @class = "form-control fixed-width" })
        </div>
    </div>
    <div class="form-group">
      @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
      <div class="col-md-10">
        @Html.TextBoxFor(m => m.Email, new { @class = "form-control fixed-width" })
      </div>
    </div>
    <p class="text-warning">Внимание! Убедитесь, что не нажат Caps Lock и раскладка клавиатуры переключена в нужное положение! 
      При вводе пароля вы должны будете использовать то же состояние Caps Lock и раскладку клавиатуры.</p>
    <div class="form-group">
        @Html.LabelFor(m => m.Password, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.PasswordFor(m => m.Password, new { @class = "form-control fixed-width" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control fixed-width" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Зарегистрироваться" />
        </div>
    </div>
}

<p>Если вы уже зарегистрированы в системе (у вас есть учетная запись), нажмите на следющую ссылку: @Html.ActionLink("Войти", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</p>
<p>Если вы были зарегистрированы, но забыли свой пароль, нажмите на следющую ссылку: @Html.ActionLink("Заменить пароль", "ForgotPassword")</p>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
